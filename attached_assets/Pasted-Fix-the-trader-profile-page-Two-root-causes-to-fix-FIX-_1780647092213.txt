Fix the trader profile page. Two root causes to fix.

FIX 1 — Backend: GET /api/users/:id/profile crashing
The server is crashing because it's trying to query tables (feedback, ads) that may not have the correct column names or relations set up. Add full error logging and safe fallbacks:
typescriptapp.get('/api/users/:id/profile', authenticate, async (req, res) => {
  try {
    const userId = parseInt(req.params.id);
    if (isNaN(userId)) {
      return res.status(400).json({ message: 'Invalid user ID' });
    }

    // Step 1: Get user — simple query first
    const user = await db.query.users.findFirst({
      where: eq(users.id, userId)
    });

    if (!user) {
      return res.status(404).json({ message: 'User not found' });
    }

    // Step 2: Get ads — wrap in try/catch separately
    let userAds = [];
    try {
      userAds = await db.query.ads.findMany({
        where: and(
          eq(ads.userId, userId),
          eq(ads.status, 'online')
        ),
        limit: 10
      });
    } catch (e) {
      console.error('Failed to fetch user ads:', e);
      userAds = [];
    }

    // Step 3: Get feedback — wrap in try/catch separately
    let userFeedback = [];
    let positiveFeedback = 0;
    let negativeFeedback = 0;
    try {
      userFeedback = await db.query.feedback.findMany({
        where: eq(feedback.toUserId, userId),
        limit: 20
      });
      positiveFeedback = userFeedback.filter(f => f.type === 'positive').length;
      negativeFeedback = userFeedback.filter(f => f.type === 'negative').length;
    } catch (e) {
      console.error('Failed to fetch feedback:', e);
    }

    // Step 4: Calculate days since registration
    const registeredDaysAgo = Math.floor(
      (Date.now() - new Date(user.createdAt).getTime()) / (1000 * 60 * 60 * 24)
    );

    // Step 5: Return safe response — no undefined values
    return res.json({
      id: user.id,
      username: user.username,
      kycStatus: user.kycStatus ?? 'pending',
      isMerchant: user.isMerchant ?? false,
      createdAt: user.createdAt,
      lastActiveAt: user.lastActiveAt ?? user.createdAt,
      registeredDaysAgo,
      verifications: {
        email: !!user.email,
        sms: !!user.phone,
        kyc: user.kycStatus === 'verified',
        address: false
      },
      stats: {
        trades30d: user.totalTrades ?? 0,
        completionRate30d: user.completionRate ?? 100,
        avgReleaseTimeMinutes: user.avgReleaseTimeMinutes ?? 0,
        avgPayTimeMinutes: user.avgPayTimeMinutes ?? 0,
        allTrades: user.totalTrades ?? 0,
        buyTrades: user.buyTrades ?? 0,
        sellTrades: user.sellTrades ?? 0,
        positiveFeedback,
        negativeFeedback,
        tradingCounterparties: user.tradingCounterparties ?? 0,
        firstTradeAt: user.firstTradeAt ?? null
      },
      ads: userAds,
      feedback: userFeedback
    });

  } catch (error) {
    console.error('Trader profile error:', error);
    return res.status(500).json({
      message: 'Internal server error',
      detail: error.message  // log detail for debugging
    });
  }
});

FIX 2 — Frontend: wrong user ID being passed to the route
The "User not found" error means the frontend is passing the wrong ID — likely passing the order ID instead of the seller's user ID.
Check everywhere trader profile is navigated to:
typescript// In src/pages/trade.tsx — WRONG (passing order id):
navigate(`/trader/${order.id}`)  // ❌ wrong

// CORRECT (pass the actual user id):
navigate(`/trader/${order.sellerId}`)  // ✅ buyer viewing seller
navigate(`/trader/${order.buyerId}`)   // ✅ seller viewing buyer

// In src/pages/p2p.tsx ad card — WRONG:
navigate(`/trader/${ad.id}`)     // ❌ wrong

// CORRECT:
navigate(`/trader/${ad.userId}`) // ✅ correct
Also make sure the route param name matches:
typescript// In App.tsx route must be:
<Route path="/trader/:userId" component={TraderProfilePage} />

// In trader-profile.tsx must read:
const { userId } = useParams<{ userId: string }>();
const numericId = parseInt(userId);
// Then fetch: /api/users/${numericId}/profile

FIX 3 — Add console.log to find exact crash point
In the backend route, add these logs so you can see exactly what's failing in the Replit console:
typescriptconsole.log('Fetching profile for userId:', userId);
console.log('User found:', !!user);
console.log('Ads count:', userAds.length);
console.log('Feedback count:', userFeedback.length);