Act as a Principal Python & Database Systems Engineer specializing in gambling engines. 

Fix my Telegram Keno Bot on Replit by implementing a Multi-Ticket Batching System that supports a single user placing multiple concurrent bets in a single round.

1. DATABASE & TRANSACTION LOGIC (Fixing Race Conditions):
- Rewrite database interaction logic to use Atomic Transactions with SQLite/SQLAlchemy (WITH db.begin():).
- Implement row-level balance checks. Calculate TOTAL_BET = sum(ticket.bet for ticket in batch).
- Enforce strict validation: IF user.balance < TOTAL_BET, ROLLBACK immediately with a zero-state execution. DEDUCT total balance in ONE single SQL command before the draw occurs.

2. MULTI-TICKET BATCH PROCESSOR:
- Create a Ticket data structure: ticket_id, user_id, picks (List[int]), bet_amount (float), status ("PENDING", "SETTLED").
- Allow a user to create N pending tickets (e.g., Ticket 1: [3, 14, 25], Ticket 2: [1, 2, 3, 4, 5, 6]).
- Implement a batch evaluator function evaluate_user_batch(user_id, ticket_batch, drawn_20_numbers):
  - Generate EXACLY ONE set of 20 drawn numbers per round using secrets.SystemRandom().sample(range(1, 81), 20).
  - Loop through every ticket in the user's batch.
  - Compute intersection: matches = set(ticket.picks) & set(drawn_20_numbers).
  - Fetch payout multiplier from paytable based on len(ticket.picks) and len(matches).
  - Calculate ticket_payout = ticket.bet * multiplier.
  - Accumulate all winnings and CREDIT the user balance in ONE atomic SQL operation.

3. REPLIT-SAFE CODE REQUIREMENTS:
- Provide clean, production-ready Python code using python-telegram-bot (v20+) or standard AsyncIO.
- Wrap all JSON/Array parsing logic in robust try-except blocks to prevent unhandled parse crashes.
- Output clean, clean markdown formatting showing:
  a) The database schema updates (User, Ticket, Draw models).
  b) The Atomic Multi-Ticket Processor function.
  c) An example Telegram inline button/menu handler that lets a user queue multiple tickets before clicking "RUN DRAW".