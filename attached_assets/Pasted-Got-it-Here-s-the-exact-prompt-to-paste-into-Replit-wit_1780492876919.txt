Got it! Here's the exact prompt to paste into Replit with the file attachment:

Replit Prompt (paste this + attach auth_v2.html)

I have attached auth_v2.html — this is the complete Auth page UI. Integrate it into the existing React project exactly as described below. Do NOT redesign or restyle anything — keep every pixel of the existing HTML/CSS/JS from the attached file.

Step 1 — Convert attached file to React
Read auth_v2.html carefully. Convert it to src/pages/auth.tsx by:

Copying all <style> CSS into a <style> tag injected via useEffect OR into a separate src/pages/auth.css file imported at the top
Converting the HTML structure to JSX (change class → className, onclick → onClick, etc.)
Converting all <script> JS functions to React state + handlers
Keep ALL animations, transitions, shapes, modal, emoji icons (📱 ✉️), country list, and sliding panel effects exactly as they are


Step 2 — Add route in App.tsx
tsximport AuthPage from "@/pages/auth";

// Add inside Router:
<Route path="/auth" component={AuthPage} />

// Redirect root to /auth if not logged in, /wallet if logged in
<Route path="/">
  {user ? <Redirect to="/wallet" /> : <Redirect to="/auth" />}
</Route>

Step 3 — Wire Login form
On Login submit call: POST /api/auth/login
json{
  "identifier": "phone number OR email",
  "password": "xxx",
  "country": "ET",
  "dialCode": "+251",
  "type": "phone"
}

On success → save JWT to localStorage key p2p_token → redirect to /wallet
On error → show error message in red below the Login button


Step 4 — Wire Register form
On Register submit call: POST /api/auth/register
json{
  "identifier": "phone number OR email",
  "password": "xxx",
  "username": "xxx",
  "country": "ET",
  "dialCode": "+251",
  "type": "phone",
  "referral": "xxx"
}

On success → save JWT → redirect to /wallet
On error → show error message in red below the Register button


Step 5 — Backend auth routes
Update artifacts/api-server/src/routes/auth.ts — add these endpoints:
POST /api/auth/register:

Validate: phone (ET must match ^[97]\d{8}$) OR valid email
Check username not already taken
Hash password with bcrypt (salt rounds: 10)
Insert into users table
Create wallet row in wallets table (available: 0, frozen: 0)
Return { token, user }

POST /api/auth/login:

Find user by phone OR email
Compare password with bcrypt.compare
Return { token, user } or 401 error

GET /api/auth/me:

Read Authorization: Bearer <token> header
Verify JWT → return user object
Return 401 if missing or invalid

Use jsonwebtoken package. JWT secret from .env as JWT_SECRET.

Step 6 — Update AuthProvider
Update src/hooks/use-auth.tsx:

Send JWT token on every API request via Authorization: Bearer <token> header
If GET /api/auth/me returns 401 → redirect to /auth
If already logged in and user visits /auth → redirect to /wallet
Remove the mock user and dev bypass completely


Step 7 — Country rules (already in the file — just keep them)

🇪🇹 Ethiopia → show 📱 Phone / ✉️ Email tabs
All other countries → ✉️ Email only, tabs hidden
ET phone validation: ^[97]\d{8}$


Step 8 — After KYC / OTP (future)
After successful register → redirect to /verify for OTP verification (build this later). For now redirect directly to /wallet.