Act as a Senior Backend & Database Systems Engineer specializing in gaming engines.

Implement a Web API Winner Calculation Engine for my Web-based Keno Application. The engine must evaluate player tickets against the 20-number draw, compute individual ticket payouts based on USD ($) bet amounts, settle database balances atomically, and return a clean JSON payload for the web frontend.

1. DATA STRUCTURES & INPUT PARSING:
- Accept a batch payload containing the 20 drawn numbers (`drawn_20_numbers`) and active player tickets.
- Each ticket object holds: `ticket_id`, `user_id`, `picks` (List[int]), `bet_amount` (float in USD), and `status`.
- Implement a Paytable lookup system that retrieves payout multiplier R(k, m) where k = len(picks) and m = len(matches).
  - Example: If a user picks 3 numbers (k=3) and hits 2 numbers (m=2), fetch the corresponding payout multiplier (e.g., 2.0x).

2. WINNER CALCULATION ENGINE LOGIC:
Write a backend settlement function `calculate_and_settle_round(drawn_20_numbers, active_tickets)`:
- Iterate through each user ticket in the batch:
  1. Calculate Matches: `hits = list(set(ticket.picks) & set(drawn_20_numbers))`
  2. Count Hits: `match_count = len(hits)`
  3. Fetch Multiplier: `multiplier = paytable.get_multiplier(picks_count=len(ticket.picks), hits_count=match_count)`
  4. Compute Individual Win Price: `ticket_payout = round(ticket.bet_amount * multiplier, 2)`
  5. Save results per ticket: `ticket.matches = hits`, `ticket.multiplier = multiplier`, `ticket.payout = ticket_payout`.

3. ATOMIC DATABASE SETTLEMENT:
- Calculate `total_user_winnings = sum(ticket.payout for ticket in user_tickets)`.
- Use an atomic database transaction (`WITH db.begin():`) to:
  - Add `total_user_winnings` to `user.balance` in ONE single SQL UPDATE statement.
  - Update all settled tickets in `game_history` with `matched_numbers`, `payout_multiplier`, `payout_amount`, and status (`SETTLED`).

4. API JSON RESPONSE FOR FRONTEND (No Telegram/Messaging Code):
Return a clean, structured API JSON response for the website frontend containing ticket details and financial summary:

{
  "round_id": 1045,
  "drawn_numbers": [3, 7, 12, 18, 22, 29, 31, 35, 42, 48, 51, 55, 60, 63, 67, 71, 74, 77, 79, 80],
  "tickets": [
    {
      "ticket_id": 102,
      "picks": [5, 18, 42],
      "matches": [18, 42],
      "hit_count": 2,
      "bet_amount": 5.00,
      "multiplier": 2.0,
      "payout": 10.00,
      "is_win": true
    },
    {
      "ticket_id": 103,
      "picks": [12, 23, 34, 56, 78],
      "matches": [12],
      "hit_count": 1,
      "bet_amount": 2.00,
      "multiplier": 0.0,
      "payout": 0.00,
      "is_win": false
    }
  ],
  "summary": {
    "total_bet": 7.00,
    "total_winnings": 10.00,
    "net_change": 3.00,
    "new_balance": 53.00
  }
}

5. CODE SAFEGUARDS:
- Wrap all financial computations with `round(amount, 2)` to eliminate floating-point precision issues.
- Ensure proper exception handling and atomic database rollbacks if any error occurs during batch processing.