Fix these 4 specific bugs. Do not touch anything else.

BUG 1 — Chat: no attachment/image upload button
In src/pages/chat.tsx or the order chat component:
Add a + attachment button next to the chat input:
typescript// Add file input (hidden) + "+" button
<input
  type="file"
  id="chat-attachment"
  accept="image/*"
  style={{ display: 'none' }}
  onChange={handleImageUpload}
/>

// "+" button opens the file picker
<button onClick={() => document.getElementById('chat-attachment').click()}>
  +
</button>

// On file selected:
const handleImageUpload = async (e) => {
  const file = e.target.files[0];
  if (!file) return;
  if (file.size > 5 * 1024 * 1024) {
    toast.error('Image must be under 5MB');
    return;
  }
  // Convert to base64 or upload to server
  const formData = new FormData();
  formData.append('image', file);
  const res = await fetch(`/api/orders/${orderId}/messages/image`, {
    method: 'POST',
    headers: { Authorization: `Bearer ${token}` },
    body: formData
  });
  const data = await res.json();
  // Show image bubble in chat
};

// Image message bubble shows the image inline:
{message.type === 'image' && (
  <img src={message.imageUrl} style={{ maxWidth: '200px', borderRadius: '8px' }} />
)}
Backend: add POST /api/orders/:id/messages/image that accepts multipart upload, saves image, returns { imageUrl }.

BUG 2 — Trader profile crash: Cannot read properties of undefined (reading 'positiveFeedback')
In src/pages/trader-profile.tsx line 142:
The stats object is undefined when the component first renders (data still loading).
Fix with optional chaining and a loading state:
typescript// Add loading guard at top of component
if (!profile || !profile.stats) {
  return (
    <div style={{ display: 'flex', justifyContent: 'center', padding: '40px' }}>
      <div>Loading...</div>
    </div>
  );
}

// Use optional chaining everywhere stats is accessed:
const totalFeedback = (profile?.stats?.positiveFeedback ?? 0) +
                      (profile?.stats?.negativeFeedback ?? 0);

const positiveRate = totalFeedback > 0
  ? ((profile?.stats?.positiveFeedback ?? 0) / totalFeedback * 100).toFixed(2)
  : '0.00';

// Also add default values in the API response:
// GET /api/users/:id/profile must always return stats object
// even if user has no trades yet:
stats: {
  trades30d: user.trades30d ?? 0,
  completionRate30d: user.completionRate30d ?? 0,
  avgReleaseTimeMinutes: user.avgReleaseTimeMinutes ?? 0,
  avgPayTimeMinutes: user.avgPayTimeMinutes ?? 0,
  allTrades: user.totalTrades ?? 0,
  buyTrades: user.buyTrades ?? 0,
  sellTrades: user.sellTrades ?? 0,
  positiveFeedback: user.positiveFeedback ?? 0,  // never undefined
  negativeFeedback: user.negativeFeedback ?? 0,  // never undefined
  tradingCounterparties: user.tradingCounterparties ?? 0,
  firstTradeAt: user.firstTradeAt ?? null,
  depositCollateral: user.depositCollateral ?? 0
}

BUG 3 — Appeal countdown is wrong (too long)
The problem: Appeal timer counts from when the ORDER was created, but it should count from when the BUYER tapped "Confirm" (marked as paid).
Fix in two places:
Backend — when buyer marks paid:
typescript// PATCH /api/orders/:id/mark-paid
// Set appeal_available_at = paid_at + 30 minutes
// NOT order created_at + payment_limit + 30 min

await db.update(orders).set({
  status: 'paid',
  paidAt: new Date(),
  // Appeal available 30 minutes after buyer marks paid
  appealAvailableAt: new Date(Date.now() + 30 * 60 * 1000)
}).where(eq(orders.id, orderId));
Frontend — appeal countdown:
typescript// Count down from appealAvailableAt
const appealTimeLeft = new Date(order.appealAvailableAt).getTime() - Date.now();

// Show: "Appeal (29:44)" — counts down to 0
// When reaches 0 → button turns orange and becomes active
const appealMinutes = Math.floor(appealTimeLeft / 60000);
const appealSeconds = Math.floor((appealTimeLeft % 60000) / 1000);

const appealLabel = appealTimeLeft > 0
  ? `Appeal (${String(appealMinutes).padStart(2,'0')}:${String(appealSeconds).padStart(2,'0')})`
  : 'Appeal';

const appealDisabled = appealTimeLeft > 0;

BUG 4 — Post Ad: payment methods must show ONLY user's saved methods + Order Limit optional
Problem A — Payment methods showing all options instead of user's saved ones:
In src/pages/ads-post.tsx Step 2, the payment methods list must come from the logged-in user's saved payment methods in their profile, NOT a hardcoded list.
typescript// Fetch user's saved payment methods
const { data: userPaymentMethods } = useQuery({
  queryKey: ['payment-methods'],
  queryFn: () => fetch('/api/payment-methods', {
    headers: { Authorization: `Bearer ${token}` }
  }).then(r => r.json())
});

// Show only methods the user has saved
// If user has no payment methods saved → show message:
// "You have no payment methods saved.
//  Add payment methods in Profile → Payment Methods first."
// With a link button: [Go to Payment Methods →]

// Each method shown as selectable tag:
// [Telebirr (Ethio Telecom)] [CBE]
// Tap to select (cyan border) — required at least 1
Validation: cannot tap "Next" without selecting at least 1 payment method:
typescriptif (selectedPaymentMethods.length === 0) {
  setError('Please select at least one payment method');
  return;
}
Problem B — Order Limit should be optional:
typescript// Order Limit fields are OPTIONAL
// If left empty → use these defaults:
const minLimit = formData.minLimit || 500;        // default 500 ETB
const maxLimit = formData.maxLimit || formData.totalAmount * formData.price; // default = full amount

// Remove any validation that blocks Next if min/max are empty
// Only validate if BOTH are filled: minLimit < maxLimit
if (formData.minLimit && formData.maxLimit) {
  if (Number(formData.minLimit) >= Number(formData.maxLimit)) {
    setError('Minimum limit must be less than maximum limit');
    return;
  }
}