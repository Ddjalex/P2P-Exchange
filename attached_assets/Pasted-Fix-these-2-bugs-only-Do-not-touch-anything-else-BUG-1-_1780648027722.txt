Fix these 2 bugs only. Do not touch anything else.

BUG 1 — Limit showing Br NaN – Br NaN
Problem: The limit shows NaN because minLimit and maxLimit are being saved as strings or null in the database, and toLocaleString() is called on a non-number.
Fix in src/pages/order-confirmation.tsx:
typescript// Safe number conversion helper — use everywhere limits are displayed:
const safeNum = (val: any) => {
  const n = Number(val);
  return isNaN(n) ? 0 : n;
};

const minLimit = safeNum(ad.minLimit);
const maxLimit = safeNum(ad.maxLimit);

// Display logic:
const limitText = (minLimit === 0 && maxLimit === 0)
  ? 'No limit'
  : `Limit Br ${minLimit.toLocaleString()} – Br ${maxLimit.toLocaleString()}`;
Fix in GET /api/ads backend — always return numbers:
typescript// When returning ads, convert to number:
return {
  ...ad,
  minLimit: Number(ad.minLimit) || 0,
  maxLimit: Number(ad.maxLimit) || 0,
  price: Number(ad.price) || 0,
  availableAmount: Number(ad.availableAmount) || 0,
  totalAmount: Number(ad.totalAmount) || 0,
}
Also fix the "Buy USDT" button disabled state:
typescript// Button enabled when:
const canSubmit =
  amount > 0 &&
  !!selectedPaymentMethod &&
  (minLimit === 0 || amount >= minLimit) &&
  (maxLimit === 0 || amount <= maxLimit);

<button disabled={!canSubmit || isLoading}>
  Buy USDT
</button>

BUG 2 — Trader profile still showing "Internal server error"
Problem: The server is crashing inside GET /api/users/:id/profile. The crash is happening because one of the database queries is using a column name that doesn't exist in the schema.
Fix — check these column names match your actual database schema:
Open your users table schema and verify these field names exist. Replace any that don't match:
typescriptapp.get('/api/users/:id/profile', authenticate, async (req, res) => {
  try {
    const userId = parseInt(req.params.id);
    if (isNaN(userId)) {
      return res.status(400).json({ message: 'Invalid user ID' });
    }

    // SIMPLE query — only columns that definitely exist:
    const user = await db.query.users.findFirst({
      where: eq(users.id, userId)
    });

    if (!user) {
      return res.status(404).json({ message: 'User not found' });
    }

    // Get ads safely
    let userAds = [];
    try {
      userAds = await db.query.ads.findMany({
        where: and(eq(ads.userId, userId), eq(ads.status, 'online')),
        limit: 10
      });
    } catch (e) {
      console.error('Ads query failed:', e.message);
    }

    // Get feedback safely
    let positiveFeedback = 0;
    let negativeFeedback = 0;
    let userFeedback = [];
    try {
      userFeedback = await db.query.feedback.findMany({
        where: eq(feedback.toUserId, userId),
        limit: 20
      });
      positiveFeedback = userFeedback.filter(f => f.type === 'positive').length;
      negativeFeedback = userFeedback.filter(f => f.type === 'negative').length;
    } catch (e) {
      console.error('Feedback query failed:', e.message);
    }

    const registeredDaysAgo = Math.floor(
      (Date.now() - new Date(user.createdAt).getTime()) / (1000 * 60 * 60 * 24)
    );

    // Return ONLY fields that definitely exist on user object:
    return res.json({
      id: user.id,
      username: user.username,
      kycStatus: user.kycStatus ?? 'pending',
      isMerchant: user.isMerchant ?? false,
      createdAt: user.createdAt,
      lastActiveAt: user.createdAt, // fallback to createdAt if lastActiveAt missing
      registeredDaysAgo,
      verifications: {
        email: !!user.email,
        sms: !!user.phone,
        kyc: user.kycStatus === 'verified',
        address: false
      },
      stats: {
        trades30d: Number(user.totalTrades) || 0,
        completionRate30d: Number(user.completionRate) || 100,
        avgReleaseTimeMinutes: Number(user.avgReleaseTimeMinutes) || 0,
        avgPayTimeMinutes: Number(user.avgPayTimeMinutes) || 0,
        allTrades: Number(user.totalTrades) || 0,
        buyTrades: 0,
        sellTrades: 0,
        positiveFeedback,
        negativeFeedback,
        tradingCounterparties: 0,
        firstTradeAt: null
      },
      ads: userAds,
      feedback: userFeedback
    });

  } catch (error) {
    // Log the FULL error so we can see what's crashing:
    console.error('=== TRADER PROFILE ERROR ===');
    console.error('userId:', req.params.id);
    console.error('error:', error.message);
    console.error('stack:', error.stack);
    return res.status(500).json({
      message: 'Internal server error',
      debug: error.message  // show in response for debugging
    });
  }
});
After applying this fix, check the Replit console log — it will print the exact error message. Share that error message with me and I'll give you the final precise fix! 🚀