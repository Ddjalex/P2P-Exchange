Build Admin Sweep Panel with real-time blockchain verification
Add a comprehensive sweep and balance verification panel to /admin/wallet or /admin/cards:

Backend endpoints needed:
GET /api/admin/wallet/hot-wallet-status:
typescript// Returns real-time hot wallet status from blockchain:
{
  hotWalletAddress: "0x24c3AaC7...",
  usdtBalance: "5.76",        // real USDT on blockchain
  bnbBalance: "0.102",        // real BNB for gas
  bnbLow: false,              // warning if < 0.02 BNB
  usdtLow: false,             // warning if < 10 USDT
}
GET /api/admin/wallet/stuck-deposits:
typescript// Scans ALL user deposit addresses on blockchain
// Returns any address holding USDT that hasn't been swept:
{
  stuckDeposits: [
    {
      userId: 4,
      email: "user@gmail.com",
      depositAddress: "0x23596...",
      usdtAmount: "9.80",
      bnbAmount: "0.001",
      lastSweepAttempt: "2026-06-30T13:32:00Z"
    }
  ],
  totalStuck: "9.80"
}
POST /api/admin/wallet/sweep-stuck-funds:
typescript// Manually triggers sweep for all stuck deposits
// Returns results per address:
{
  results: [
    {
      userId: 4,
      address: "0x23596...",
      amount: "9.80",
      success: true,
      txid: "0x..."
    }
  ],
  swept: 1,
  failed: 0
}
GET /api/admin/wallet/balance-audit:
typescript// Compares platform balances vs real hot wallet:
{
  hotWalletUsdt: "5.76",          // real USDT on blockchain
  totalPlatformBalance: "2046.09", // sum of all user balances
  totalRealDeposits: "62.78",      // total BEP20/TRC20 deposited
  totalWithdrawn: "39.00",         // total withdrawn
  expectedHotWallet: "23.78",      // real deposits - withdrawals
  discrepancy: "-18.02",           // difference (negative = deficit)
  suspiciousUsers: [               // users with balance > real deposits
    {
      userId: 210,
      email: "jintom2406@gmail.com",
      platformBalance: "984605",
      realDeposits: "20.00",
      difference: "984585"         // impossible balance!
    }
  ]
}

Frontend — Add "Wallet Health" panel to /admin/wallet:
💰 Hot Wallet Status
─────────────────────────────────────────
Address: 0x24c3AaC7...321B3 [Copy] [BSCScan ↗]
USDT Balance: $5.76          ⚠️ Low — top up recommended
BNB Balance:  0.102 BNB      ✅ Good
─────────────────────────────────────────

🔍 Stuck Deposits Scanner
─────────────────────────────────────────
[🔄 Scan Now]
Last scanned: 2 minutes ago

Stuck deposits found: 0 ✅
Total stuck: $0.00

If stuck deposits found show:
User    Address          Amount    Action
User 4  0x23596...       $9.80    [Sweep]
─────────────────────────────────────────
[🧹 Sweep All Stuck Funds]

📊 Balance Audit
─────────────────────────────────────────
Hot wallet real USDT:     $5.76
Total platform balances:  $2,046.09
Real chain deposits:      $62.78
Total withdrawn:          $39.00
Expected hot wallet:      $23.78
Actual hot wallet:        $5.76
Discrepancy:              -$18.02

⚠️ Suspicious Users (balance >> real deposits):
User 210 — jintom2406@gmail.com
  Platform balance: $984,605
  Real deposits:    $20.00
  Difference:       $984,585 🚨
  [🔒 Freeze] [❌ Ban] [📋 View]
─────────────────────────────────────────
[🔄 Refresh All]

Auto-alerts when:

Hot wallet USDT < $10 → push notification to admin
Hot wallet BNB < 0.02 → push notification to admin
Any user balance > $10,000 → immediate alert
Stuck deposit detected > 1 hour → alert admin
Real deposits vs platform balance discrepancy > $100 → alert admin


Also add to the suspicious pattern detector (runs every 5 min):
typescript// Check if any user's platform balance is impossibly higher than their real deposits:
const suspiciousUsers = await db.execute(sql`
  SELECT 
    w.user_id,
    u.email,
    w.available_balance::numeric + w.frozen_balance::numeric as platform_balance,
    COALESCE(real_deps.total, 0) as real_deposits
  FROM wallets w
  JOIN users u ON u.id = w.user_id
  LEFT JOIN (
    SELECT user_id, SUM(amount::numeric) as total
    FROM transactions
    WHERE type = 'deposit'
    AND network IN ('BEP20', 'TRC20')
    AND status = 'completed'
    GROUP BY user_id
  ) real_deps ON real_deps.user_id = w.user_id
  WHERE (w.available_balance::numeric + w.frozen_balance::numeric) > 
        COALESCE(real_deps.total, 0) + 1000
  AND (w.available_balance::numeric + w.frozen_balance::numeric) > 100
`);

for (const user of suspiciousUsers.rows) {
  console.error(`[Security] SUSPICIOUS: User ${user.user_id} (${user.email}) has platform balance $${user.platform_balance} but only deposited $${user.real_deposits} on chain!`);
  await PushNotify.adminAlert(`🚨 SUSPICIOUS BALANCE: ${user.email} has $${user.platform_balance} platform balance but only $${user.real_deposits} real deposits!`);
}